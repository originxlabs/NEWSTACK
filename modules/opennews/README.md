# OpenNews Module

This directory defines the independent OpenNews engine contract for extraction as a standalone package.
Runtime integration currently lives in `src/modules/opennews` for Vite beta 1.0 compatibility.

Subfolders mirror production boundaries:
- api
- components
- moderation
- politics
- routes
- hooks
- lib
- types
- services
- state
